# Base URL of the FastAPI backend. Leave unset in local dev to use the Vite
# proxy (/api → http://localhost:8000). Set in production / on InsForge deploy.
VITE_API_URL=https://rag-assist-api-2c425752-0061-4a84-8efd-8af5b5022cbe.fly.dev
